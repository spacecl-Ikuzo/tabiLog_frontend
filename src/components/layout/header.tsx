import TopNavigation from './top-navigation';

const Header = () => {
  return (
    <header className="h-[40px] bg-white p-[30px]">
      <TopNavigation />
    </header>
  );
};

export default Header;
